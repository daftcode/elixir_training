defmodule Exercises.Tuples do
"""
Returns bigger tuple from given params. Return second if equal
"""
  def bigger_tuple(tuple1, tuple2) do
  end

  def last_element(tuple) do
  end

  def add_element_at_start_and_end(tup, new_elem) do
  end


end
